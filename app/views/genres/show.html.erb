<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @genre.name %>
</p>

<p>
  Artists in the <%= @genre.name %> genre include:
</p>

<ul>
  <% @genre.artists.each do |artist| %>
  <li><%= link_to artist.name, artist_path(artist) %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_genre_path(@genre) %> |
<%= link_to 'Back', genres_path %>
